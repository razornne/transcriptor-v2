.env
recordings/
__pycache__/
*.pyc
*.pyo
venv/
.venv/
.claude/

# HuggingFace кеш моделей (если попадёт в проект)
.cache/

# Eval audio (generated / downloaded from the archive) — never commit
eval_set/
