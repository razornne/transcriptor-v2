{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Settings window and dictation overlay",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
